# Real Gate 3 API is the default. Point this at the loopback API port in use.
VITE_COVENANT_API_URL=http://127.0.0.1:8000

# Optional explicit design-preview mode. Never use as a production fallback.
# VITE_COVENANT_DATA_MODE=fixture
