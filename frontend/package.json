{
  "name": "covenant-record",
  "version": "1.0.0",
  "private": true,
  "description": "Covenant Record — locked Record visual system + layered causal graph integrated with the Gate 3 API.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "node scripts/browser-check.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^6.0.4",
    "playwright-core": "^1.62.0",
    "typescript": "^5.4.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
