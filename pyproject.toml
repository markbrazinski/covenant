[project]
name = "covenant-native-semantic-graph"
version = "0.2.0"
description = "DataHub-backed native semantic graph proof for Covenant"
requires-python = ">=3.11,<3.12"
dependencies = [
  "acryl-datahub==1.6.0",
  "mcp-server-datahub==0.6.0",
  "PyYAML==6.0.3",
]

[project.optional-dependencies]
test = ["pytest==8.4.1"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
